/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WifiNetwork } from '../../models/wifi-network';

export interface ScanWifiNetworks$Params {
}

export function scanWifiNetworks(http: HttpClient, rootUrl: string, params?: ScanWifiNetworks$Params, context?: HttpContext): Observable<StrictHttpResponse<{
'networks': Array<WifiNetwork>;
}>> {
  const rb = new RequestBuilder(rootUrl, scanWifiNetworks.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      'networks': Array<WifiNetwork>;
      }>;
    })
  );
}

scanWifiNetworks.PATH = '/api/system/wifi/scan';
