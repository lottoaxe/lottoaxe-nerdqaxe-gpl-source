/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface DismissBlockFound$Params {
}

export function dismissBlockFound(http: HttpClient, rootUrl: string, params?: DismissBlockFound$Params, context?: HttpContext): Observable<StrictHttpResponse<{

/**
 * Number of blocks found
 */
'blockFound'?: number;

/**
 * Whether to show the block found notification
 */
'showNewBlock'?: boolean;
'message'?: string;
}>> {
  const rb = new RequestBuilder(rootUrl, dismissBlockFound.PATH, 'post');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      
      /**
       * Number of blocks found
       */
      'blockFound'?: number;
      
      /**
       * Whether to show the block found notification
       */
      'showNewBlock'?: boolean;
      'message'?: string;
      }>;
    })
  );
}

dismissBlockFound.PATH = '/api/system/blockFound/dismiss';
