/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SystemStatistics } from '../../models/system-statistics';

export interface GetSystemStatistics$Params {

/**
 * List of labels for which data should be retrieved
 */
  columns?: Array<string>;
}

export function getSystemStatistics(http: HttpClient, rootUrl: string, params?: GetSystemStatistics$Params, context?: HttpContext): Observable<StrictHttpResponse<SystemStatistics>> {
  const rb = new RequestBuilder(rootUrl, getSystemStatistics.PATH, 'get');
  if (params) {
    rb.query('columns', params.columns, {"style":"form","explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SystemStatistics>;
    })
  );
}

getSystemStatistics.PATH = '/api/system/statistics';
