/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SystemAsic } from '../../models/system-asic';

export interface GetAsicSettings$Params {
}

export function getAsicSettings(http: HttpClient, rootUrl: string, params?: GetAsicSettings$Params, context?: HttpContext): Observable<StrictHttpResponse<SystemAsic>> {
  const rb = new RequestBuilder(rootUrl, getAsicSettings.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SystemAsic>;
    })
  );
}

getAsicSettings.PATH = '/api/system/asic';
